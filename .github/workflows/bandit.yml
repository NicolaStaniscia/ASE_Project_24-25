name: Static Analysis

on:
  push:
    branches:
      - main
      
jobs:
  bandit-users:
    name: Account Management Analysis
    runs-on: ubuntu-latest 

    steps:
      # Repo checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      # Bandit installation
      - name: Install Bandit
        run: pip install bandit

      # Bandit execution
      - name: Run Bandit
        run: bandit -r ./src/users/ -f txt -o bandit-report.txt

      # Report upload
      - name: Upload Bandit report
        uses: actions/upload-artifact@v3
        with:
          name: account-management-bandit
          path: bandit-report.txt

  bandit-collection:
    name: Collection Analysis
    runs-on: ubuntu-latest 

    steps:
      # Repo checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      # Bandit installation
      - name: Install Bandit
        run: pip install bandit

      # Bandit execution
      - name: Run Bandit
        run: bandit -r ./src/collection/ -f txt -o bandit-report.txt

      # Report upload
      - name: Upload Bandit report
        uses: actions/upload-artifact@v3
        with:
          name: collection-bandit
          path: bandit-report.txt
          
  bandit-market:
    name: Market Analysis
    runs-on: ubuntu-latest 

    steps:
      # Repo checkout
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      # Bandit installation
      - name: Install Bandit
        run: pip install bandit

      # Bandit execution
      - name: Run Bandit
        run: bandit -r ./src/market/ -f txt -o bandit-report.txt

      # Report upload
      - name: Upload Bandit report
        uses: actions/upload-artifact@v3
        with:
          name: market-bandit
          path: bandit-report.txt
